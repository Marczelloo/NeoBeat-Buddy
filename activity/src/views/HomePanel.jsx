import { MagnifyingGlass, Sparkle, SpinnerGap } from "@phosphor-icons/react";

export default function HomePanel({ onView, onAction, isActionPending = () => false, loading = false }) {
  if (loading) return <section className="home-panel home-panel-loading panel-surface" aria-busy="true" aria-label="Synchronizing MewBit Activity"><div className="home-orbit" aria-hidden="true"><span /><span /><span /></div><div className="home-loading-copy"><span /><strong /><strong /><i /><i /><div><b /><b /></div></div><div className="home-signal" aria-hidden="true"><span /><span /><span /><span /><span /><span /><span /></div></section>;
  return <section className="home-panel home-panel-idle panel-surface"><div className="home-idle-copy"><h1>Start the room<br /><em>your way.</em></h1><p>Play something specific, or let MewBit find a verified pick from your taste — with a current-chart fallback for a fresh room.</p><div className="home-actions"><button className="surprise-button home-freestyle-action" type="button" onClick={() => onAction("surprise_me")} disabled={isActionPending("surprise_me")} aria-busy={isActionPending("surprise_me") || undefined}>{isActionPending("surprise_me") ? <SpinnerGap className="button-spinner" size={17} aria-hidden="true" /> : <Sparkle size={17} weight="fill" aria-hidden="true" />}{isActionPending("surprise_me") ? "Finding your next pick..." : "Surprise me"}</button><button className="home-search-action" type="button" onClick={() => onView("search")}><MagnifyingGlass size={17} weight="bold" aria-hidden="true" /> Search music</button></div></div></section>;
}
